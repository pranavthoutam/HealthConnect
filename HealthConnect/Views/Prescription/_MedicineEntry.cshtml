@* @model PrescribedMedicineDetail

<div class="medicine-entry row g-3 mb-3">
    <div class="col-md-6">
        <label for="medicine-name" class="form-label">Medicine Name</label>
        <input type="text" name="Medicines[@ModelIndex].MedicineName" class="form-control" placeholder="Enter medicine name" value="@Model.MedicineName" />
    </div>

    <div class="col-md-6">
        <label class="form-label d-block">Timings</label>
        <div class="form-check form-check-inline">
            <input class="form-check-input" type="checkbox" name="Medicines[@ModelIndex].Timings" value="all" @if (Model.Timings.Contains("all"))
            {
                <text>checked</text>
            }>
            <label class="form-check-label" for="all">All</label>
        </div>
        <div class="form-check form-check-inline">
            <input class="form-check-input" type="checkbox" name="Medicines[@ModelIndex].Timings" value="morning" @if (Model.Timings.Contains("morning"))
            {
                <text>checked</text>
            }>
            <label class="form-check-label" for="morning">Morning</label>
        </div>
        <div class="form-check form-check-inline">
            <input class="form-check-input" type="checkbox" name="Medicines[@ModelIndex].Timings" value="afternoon" @if (Model.Timings.Contains("afternoon"))
            {
                <text>checked</text>
            }>
            <label class="form-check-label" for="afternoon">Afternoon</label>
        </div>
        <div class="form-check form-check-inline">
            <input class="form-check-input" type="checkbox" name="Medicines[@ModelIndex].Timings" value="night" @if (Model.Timings.Contains("night"))
            {
                <text>checked</text>
            }>
            <label class="form-check-label" for="night">Night</label>
        </div>
    </div>

    <div class="col-md-6">
        <label class="form-label">Food Instructions</label>
        <div class="form-check">
            <input class="form-check-input" type="radio" name="Medicines[@ModelIndex].FoodInstructions" value="before" @if (Model.FoodInstructions == "before")
            {
                <text>checked</text>
            }>
            <label class="form-check-label" for="before-food">Before Food</label>
        </div>
        <div class="form-check">
            <input class="form-check-input" type="radio" name="Medicines[@ModelIndex].FoodInstructions" value="after" @if (Model.FoodInstructions == "after")
            {
                <text>checked</text>
            }>
            <label class="form-check-label" for="after-food">After Food</label>
        </div>
    </div>

    <div class="col-md-6">
        <label for="additional-advice" class="form-label">Additional Advice</label>
        <textarea name="Medicines[@ModelIndex].AdditionalAdvice" id="additional-advice" class="form-control" rows="3" placeholder="Enter any additional advice">@Model.AdditionalAdvice</textarea>
    </div>
</div>
 *@